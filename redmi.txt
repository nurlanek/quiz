

composer require laravel/jetstream
php artisan jetstream:install livewire
npm install
npm run dev
npm run watch
php artisan storage:link \\ resim klasorlerinin yolunu duzenleme. Profil fotosunu sirasinda yaptim.

npm install bootstrap

resources/css/app.css te
@import 'bootstrap'; eklemek gerek
npm run dev


migrate yaptigimiz zaman hata verdiyse
Update your /app/Providers/AppServiceProvider.php to contain:
use Illuminate\Support\Facades\Schema;

/**
 * Bootstrap any application services.
 *
 * @return void
 */
public function boot()
{
    Schema::defaultStringLength(191);
}

for table refresh
php artisan migrate:fresh

daha sonra teams calistirmak icin bu yaptim
php artisan jetstream:install livewire --teams
